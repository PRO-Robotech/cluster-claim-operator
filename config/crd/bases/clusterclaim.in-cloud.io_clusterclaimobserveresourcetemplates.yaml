---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterclaimobserveresourcetemplates.clusterclaim.in-cloud.io
spec:
  group: clusterclaim.in-cloud.io
  names:
    kind: ClusterClaimObserveResourceTemplate
    listKind: ClusterClaimObserveResourceTemplateList
    plural: clusterclaimobserveresourcetemplates
    singular: clusterclaimobserveresourcetemplate
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.apiVersion
      name: APIVersion
      type: string
    - jsonPath: .spec.kind
      name: Kind
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterClaimObserveResourceTemplate is the Schema for the clusterclaimobserveresourcetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterClaimObserveResourceTemplateSpec defines the desired
              state of ClusterClaimObserveResourceTemplate.
            properties:
              apiVersion:
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: apiVersion is immutable
                  rule: self == oldSelf
              kind:
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: kind is immutable
                  rule: self == oldSelf
              value:
                minLength: 1
                type: string
            required:
            - apiVersion
            - kind
            - value
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
