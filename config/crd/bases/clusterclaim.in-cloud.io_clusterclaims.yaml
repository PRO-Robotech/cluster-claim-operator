---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: clusterclaims.clusterclaim.in-cloud.io
spec:
  group: clusterclaim.in-cloud.io
  names:
    kind: ClusterClaim
    listKind: ClusterClaimList
    plural: clusterclaims
    singular: clusterclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ClusterClaim is the Schema for the clusterclaims API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ClusterClaimSpec defines the desired state of ClusterClaim.
            properties:
              ccmCsrTemplateRef:
                description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              certificateSetTemplateRef:
                description: DualTemplateRef holds infra and optional client template
                  references.
                properties:
                  client:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  infra:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                required:
                - infra
                type: object
              client:
                description: ClientSpec defines the client cluster configuration.
                properties:
                  componentVersions:
                    additionalProperties:
                      description: ComponentVersion defines a version for a cluster
                        component.
                      properties:
                        version:
                          minLength: 1
                          type: string
                      required:
                      - version
                      type: object
                    type: object
                  enabled:
                    type: boolean
                    x-kubernetes-validations:
                    - message: client.enabled is immutable
                      rule: self == oldSelf
                  network:
                    description: NetworkConfig defines network settings for a cluster.
                    properties:
                      clusterDNS:
                        minLength: 1
                        type: string
                      kubeApiserverPort:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      podCidr:
                        minLength: 1
                        type: string
                      podCidrMaskSize:
                        format: int32
                        type: integer
                      serviceCidr:
                        minLength: 1
                        type: string
                    required:
                    - clusterDNS
                    - kubeApiserverPort
                    - podCidr
                    - serviceCidr
                    type: object
                  paused:
                    type: boolean
                required:
                - enabled
                type: object
              clusterTemplateRef:
                description: DualTemplateRef holds infra and optional client template
                  references.
                properties:
                  client:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  infra:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                required:
                - infra
                type: object
              configMapTemplateRef:
                description: DualTemplateRef holds infra and optional client template
                  references.
                properties:
                  client:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  infra:
                    description: TemplateRef is a reference to a cluster-scoped ClusterClaimObserveResourceTemplate.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                required:
                - infra
                type: object
              configuration:
                description: ConfigurationSpec defines compute resource configuration
                  for control plane nodes.
                properties:
                  cpuCount:
                    format: int32
                    minimum: 1
                    type: integer
                  diskSize:
                    format: int32
                    minimum: 1
                    type: integer
                  memory:
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - cpuCount
                - diskSize
                - memory
                type: object
              extraEnvs:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
              infra:
                description: InfraSpec defines the infra cluster configuration.
                properties:
                  componentVersions:
                    additionalProperties:
                      description: ComponentVersion defines a version for a cluster
                        component.
                      properties:
                        version:
                          minLength: 1
                          type: string
                      required:
                      - version
                      type: object
                    type: object
                  network:
                    description: NetworkConfig defines network settings for a cluster.
                    properties:
                      clusterDNS:
                        minLength: 1
                        type: string
                      kubeApiserverPort:
                        format: int32
                        maximum: 65535
                        minimum: 1
                        type: integer
                      podCidr:
                        minLength: 1
                        type: string
                      podCidrMaskSize:
                        format: int32
                        type: integer
                      serviceCidr:
                        minLength: 1
                        type: string
                    required:
                    - clusterDNS
                    - kubeApiserverPort
                    - podCidr
                    - serviceCidr
                    type: object
                  paused:
                    type: boolean
                  role:
                    minLength: 1
                    type: string
                required:
                - componentVersions
                - network
                - paused
                - role
                type: object
              observeTemplateRef:
                description: Template references.
                properties:
                  name:
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              remoteNamespace:
                type: string
              replicas:
                format: int32
                minimum: 1
                type: integer
            required:
            - ccmCsrTemplateRef
            - certificateSetTemplateRef
            - client
            - clusterTemplateRef
            - configuration
            - infra
            - observeTemplateRef
            - replicas
            type: object
          status:
            description: ClusterClaimStatus defines the observed state of ClusterClaim.
            properties:
              clusters:
                description: ClustersStatus holds status summaries for infra and client
                  clusters.
                properties:
                  client:
                    description: ClusterStatusSummary mirrors status fields from a
                      CAPI Cluster object.
                    properties:
                      conditions:
                        items:
                          description: Condition contains details for one aspect of
                            the current state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False,
                                Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - type
                        x-kubernetes-list-type: map
                      controlPlane:
                        description: ReplicaStatus contains replica count information
                          mirrored from a CAPI Cluster.
                        properties:
                          availableReplicas:
                            format: int32
                            type: integer
                          desiredReplicas:
                            format: int32
                            type: integer
                          readyReplicas:
                            format: int32
                            type: integer
                          replicas:
                            format: int32
                            type: integer
                          upToDateReplicas:
                            format: int32
                            type: integer
                        type: object
                      phase:
                        type: string
                      workers:
                        description: ReplicaStatus contains replica count information
                          mirrored from a CAPI Cluster.
                        properties:
                          availableReplicas:
                            format: int32
                            type: integer
                          desiredReplicas:
                            format: int32
                            type: integer
                          readyReplicas:
                            format: int32
                            type: integer
                          replicas:
                            format: int32
                            type: integer
                          upToDateReplicas:
                            format: int32
                            type: integer
                        type: object
                    type: object
                  infra:
                    description: ClusterStatusSummary mirrors status fields from a
                      CAPI Cluster object.
                    properties:
                      conditions:
                        items:
                          description: Condition contains details for one aspect of
                            the current state of this API Resource.
                          properties:
                            lastTransitionTime:
                              description: |-
                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                              format: date-time
                              type: string
                            message:
                              description: |-
                                message is a human readable message indicating details about the transition.
                                This may be an empty string.
                              maxLength: 32768
                              type: string
                            observedGeneration:
                              description: |-
                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                with respect to the current state of the instance.
                              format: int64
                              minimum: 0
                              type: integer
                            reason:
                              description: |-
                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                Producers of specific condition types may define expected values and meanings for this field,
                                and whether the values are considered a guaranteed API.
                                The value should be a CamelCase string.
                                This field may not be empty.
                              maxLength: 1024
                              minLength: 1
                              pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                              type: string
                            status:
                              description: status of the condition, one of True, False,
                                Unknown.
                              enum:
                              - "True"
                              - "False"
                              - Unknown
                              type: string
                            type:
                              description: type of condition in CamelCase or in foo.example.com/CamelCase.
                              maxLength: 316
                              pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                              type: string
                          required:
                          - lastTransitionTime
                          - message
                          - reason
                          - status
                          - type
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - type
                        x-kubernetes-list-type: map
                      controlPlane:
                        description: ReplicaStatus contains replica count information
                          mirrored from a CAPI Cluster.
                        properties:
                          availableReplicas:
                            format: int32
                            type: integer
                          desiredReplicas:
                            format: int32
                            type: integer
                          readyReplicas:
                            format: int32
                            type: integer
                          replicas:
                            format: int32
                            type: integer
                          upToDateReplicas:
                            format: int32
                            type: integer
                        type: object
                      phase:
                        type: string
                      workers:
                        description: ReplicaStatus contains replica count information
                          mirrored from a CAPI Cluster.
                        properties:
                          availableReplicas:
                            format: int32
                            type: integer
                          desiredReplicas:
                            format: int32
                            type: integer
                          readyReplicas:
                            format: int32
                            type: integer
                          replicas:
                            format: int32
                            type: integer
                          upToDateReplicas:
                            format: int32
                            type: integer
                        type: object
                    type: object
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                format: int64
                type: integer
              phase:
                enum:
                - Provisioning
                - WaitingDependency
                - Ready
                - Failed
                - Degraded
                - Paused
                - Deleting
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
