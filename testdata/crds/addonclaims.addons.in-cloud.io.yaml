---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: addonclaims.addons.in-cloud.io
spec:
  group: addons.in-cloud.io
  names:
    kind: AddonClaim
    listKind: AddonClaimList
    plural: addonclaims
    singular: addonclaim
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.addon.name
      name: Addon
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          AddonClaim represents a request to deploy an addon in an infra cluster.
          The controller renders the referenced AddonTemplate with the claim's values
          and creates the corresponding Addon and AddonValue in the remote cluster.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AddonClaimSpec defines the desired state of an AddonClaim.
            properties:
              addon:
                description: |-
                  Addon specifies the identity of the remote Addon resource.
                  The name is required and immutable — it cannot be changed after creation.
                  The rendered template's metadata.name is overridden by this value.
                properties:
                  name:
                    description: Name of the Addon resource in the remote cluster.
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              credentialRef:
                description: |-
                  CredentialRef references the Secret containing the infra cluster kubeconfig.
                  The Secret must have a "value" key with the kubeconfig data.
                properties:
                  name:
                    description: 'Name of the Secret containing the kubeconfig (key:
                      "value").'
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              templateRef:
                description: TemplateRef references the AddonTemplate to use for rendering.
                properties:
                  name:
                    description: Name of the AddonTemplate.
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              valueLabels:
                default: claim
                description: |-
                  ValueLabels overrides the "addons.in-cloud.io/values" label on the generated AddonValue.
                  Defaults to "claim". Used to match custom valuesSelectors in the rendered Addon.
                maxLength: 63
                type: string
              values:
                description: |-
                  Values provides structured Helm values as a JSON object.
                  Mutually exclusive with ValuesString — only one may be set.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              valuesString:
                description: |-
                  ValuesString provides Helm values as a YAML string.
                  May contain Go template expressions rendered with the AddonClaim context.
                  Mutually exclusive with Values — only one may be set.
                type: string
              variables:
                description: |-
                  Variables provides arbitrary parameters for template rendering.
                  Accessible in templates as .Vars.<key> (shortcut) or .Values.spec.variables.<key>.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              version:
                description: |-
                  Version specifies the desired version (e.g. Kubernetes version for control plane addons).
                  Set by CAPI when cloning from a template.
                  When annotation "external-status/type" is present, copied to status.version.
                type: string
            required:
            - addon
            - credentialRef
            - templateRef
            type: object
            x-kubernetes-validations:
            - message: values and valuesString are mutually exclusive
              rule: '!(has(self.values) && size(self.valuesString) > 0)'
            - message: spec.addon.name is immutable
              rule: self.addon.name == oldSelf.addon.name
          status:
            description: AddonClaimStatus defines the observed state of an AddonClaim.
            properties:
              conditions:
                description: Conditions represent the current state of the AddonClaim.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deployed:
                description: Deployed indicates the remote Addon has been deployed
                  at least once.
                type: boolean
              externalManagedControlPlane:
                description: |-
                  ExternalManagedControlPlane indicates the control plane is externally managed.
                  Populated only when annotation "external-status/type" is present.
                type: boolean
              initialization:
                description: Initialization contains CAPI v1beta2 initialization status.
                properties:
                  controlPlaneInitialized:
                    description: ControlPlaneInitialized indicates the first control
                      plane instance is ready.
                    type: boolean
                type: object
              initialized:
                description: |-
                  Initialized indicates the control plane has been initialized (CAPI v1beta1, deprecated).
                  Reflects status.deployed (latching bool) from the remote Addon.
                  Populated only when annotation "external-status/type" is present.
                type: boolean
              observedGeneration:
                description: ObservedGeneration is the last spec.generation processed
                  by the controller.
                format: int64
                type: integer
              ready:
                description: Ready indicates the remote Addon is fully reconciled
                  and healthy.
                type: boolean
              remoteAddonStatus:
                description: RemoteAddonStatus mirrors the status of the Addon in
                  the infra cluster.
                properties:
                  conditions:
                    description: Conditions from the remote Addon.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    type: array
                  deployed:
                    description: Deployed indicates the remote Addon has been deployed.
                    type: boolean
                  generation:
                    description: Generation is metadata.generation of the remote Addon
                      at read time.
                    format: int64
                    type: integer
                  observedGeneration:
                    description: |-
                      ObservedGeneration is status.observedGeneration of the remote Addon at read time.
                      Conditions describe a previous revision unless it equals Generation.
                    format: int64
                    type: integer
                type: object
              version:
                description: |-
                  Version reflects spec.version, published only once the remote Addon is confirmed
                  reconciled at its current generation. Never cleared once set.
                  Populated only when annotation "external-status/type" is present.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
